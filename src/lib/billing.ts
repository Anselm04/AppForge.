export type AppForgePlan='starter'|'builder'|'studio';export async function startCheckout(plan:AppForgePlan,accessToken:string){const r=await fetch('/api/checkout',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${accessToken}`},body:JSON.stringify({plan})}),x=await r.json();if(!r.ok)throw new Error(x.error||'Unable to start checkout.');window.location.assign(x.url)}